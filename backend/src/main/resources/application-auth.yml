app:
  service:
    mode: auth

spring:
  application:
    name: auth-service

server:
  port: ${AUTH_SERVICE_PORT:8082}

---
spring:
  config:
    activate:
      on-profile: test
server:
  port: ${AUTH_SERVICE_TEST_PORT:18082}

---
spring:
  config:
    activate:
      on-profile: prod
server:
  port: ${AUTH_SERVICE_PORT:8082}
